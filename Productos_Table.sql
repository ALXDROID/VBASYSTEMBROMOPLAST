CREATE TABLE Productos (
    IDProd INTEGER,
    nombre VARCHAR(255),
    stockMinimo TEXT,
    descripcion TEXT,
    stockActual TEXT,
    precioUnidad TEXT,
    proveedor INTEGER,
    categoria INTEGER,
    imagen VARCHAR(255),
    disponible BOOLEAN
);
